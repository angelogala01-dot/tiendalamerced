[build]
builder = "NIXPACKS"
watchPatterns = ["admin/**"]

[deploy]
startCommand = "npx next start -H 0.0.0.0 -p ${PORT:-3001}"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
